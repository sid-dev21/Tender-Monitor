"""Scraper-test fixtures.

The local HTTP server fixture (`http_server`) and `LocalServer` now live in the
root tests/conftest.py so every test package can use them. Import LocalServer from
there: `from tests.conftest import LocalServer`.
"""
