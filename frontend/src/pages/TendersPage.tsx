import { PagePlaceholder } from '../components/PagePlaceholder'

export function TendersPage() {
  return (
    <PagePlaceholder
      title="Tenders"
      description="Browse and filter extracted tenders by keyword, sector, and deadline."
      backendPhase="the tenders API (Phase 8)"
    />
  )
}
