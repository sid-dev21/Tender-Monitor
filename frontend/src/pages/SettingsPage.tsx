import { PagePlaceholder } from '../components/PagePlaceholder'

export function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Manage your keywords and notification email recipients (max 5)."
      backendPhase="the users API (Phase 8 & 9)"
    />
  )
}
