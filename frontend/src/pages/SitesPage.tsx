import { PagePlaceholder } from '../components/PagePlaceholder'

export function SitesPage() {
  return (
    <PagePlaceholder
      title="Sources"
      description="Add, edit, and test the source URLs you monitor. Adding a site is just data — no code."
      backendPhase="the sites API (Phase 7)"
    />
  )
}
